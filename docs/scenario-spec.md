# ことほぎ — シナリオテスト仕様書

| 項目 | 内容 |
|------|------|
| プロダクト名 | ことほぎ（Kotohogi） |
| 文書バージョン | 1.2 |
| 最終更新 | 2026-07-23 |
| この文書の役割 | **利用者の一連の操作（シナリオ）をどう確認するか**の正本 |
| 自動テストの全体方針 | [test-spec.md](./test-spec.md) |
| コマンドの打ち方・仕組み | [ops/testing.md](./ops/testing.md) |

関連: [要件](./requirements.md) · [設計](./design.md) · [docs 目次](./README.md)

---

## 0. この文書の読み方

**シナリオテスト**とは、「ユーザーが画面上でこういう順番で操作したとき、こうなるはず」という**物語単位**の確認です。  
「部品が正しいか」は単体テスト（L1）、「通信が生きているか」はスモーク（L2）の領域です。この文書は **L2b（E2E）** の合格条件の正本です。

| 似ている言葉 | ちがい |
|--------------|--------|
| 単体テスト（L1） | 部品（関数）だけを見る。画面は開かない |
| スモーク（L2） | 通信が通るかの短い確認。操作の物語はほぼ無い |
| **シナリオ／E2E（L2b）** | ブラウザで一連の操作をする。**この文書の主役** |
| 手動確認（L3） | 人がプレビューで触る。自動化しにくい・稀な経路向け |

自動実行は **ガーキン記法**（「前提・もし・ならば」）の `.feature` ファイルと、Playwright のステップ実装で行います。結果は **動画（webm）** で後から見られます。

| 読みたいこと | 読む場所 |
|--------------|----------|
| シナリオ一覧と合格条件 | この文書 §3 以降 |
| 用語（E2E・ガーキンなど） | [test-spec.md 用語集](./test-spec.md#1-用語集この文書で使う言葉) |
| 実行コマンド・動画・pre-push | [ops/testing.md](./ops/testing.md) |
| 要件の受け入れとの対応 | この文書 §1 · [requirements.md §7](./requirements.md) |

### 0.1 関係資材マップ

| 種類 | パス | 役割 |
|------|------|------|
| この仕様書 | `docs/scenario-spec.md` | SC-* の合格条件（正） |
| ガーキン（シナリオ文） | `e2e/features/*.feature` | 日本語の物語 |
| ステップ実装 | `e2e/steps/app.steps.ts` | 「もし〜」の実際の操作 |
| 共通準備 | `e2e/fixtures.ts` | Playwright-BDD の土台 |
| Playwright 設定 | `playwright.config.ts` | ポート 3100・動画録画など |
| ローカル動画 | `test-results/**/*.webm` | 実行後の録画（Git 管理外） |
| HTML レポート | `playwright-report/` | `npm run test:e2e:report` |
| CI Artifact | GitHub Actions の `e2e-videos` | CI 上の録画＋レポート |
| 全体方針 | `docs/test-spec.md` §8 | L2b の位置づけ |
| 実行手順 | `docs/ops/testing.md` | コマンド・トラブルシュート |

---

## 1. 目的

要件の受け入れ条件（抜粋）を、**再現可能なシナリオ**として定義し、可能なものは自動で毎回確認する。

| 受け入れ（要件 §7） | 対応シナリオ ID |
|---------------------|-----------------|
| 1. LP から3アプリに遷移できる | SC-LP-02 |
| 2. ビンゴで名前→達成→日時 | SC-BINGO-01 |
| 3. クイズを URL 共有し別端末で同じ問題 | SC-QUIZ-01 |
| 4. Host→ゲストURL→投票→結果表示 | SC-POLL-01 · SC-POLL-02 |
| 5. Host が押しても票が増えない | SC-POLL-03 |
| 6. デプロイ後の本番再現 | （手動 L3／本番。自動は非本番向け） |

---

## 2. 方針

| ID | 方針 |
|----|------|
| SC-P01 | シナリオは **利用者視点の言葉**で書き、実装の内部名に依存しすぎない |
| SC-P02 | 自動可能なものは **ガーキン + Playwright** で L2b として実行し、**動画を残す** |
| SC-P03 | テスト用ルームは実行ごとに新規発行（アンケートは自動生成）。本番イベントとぶつからないようにする |
| SC-P04 | 本番 URL・本番 KV はシナリオ自動実行の対象にしない |
| SC-P05 | 1 シナリオは **1つの利用者ゴール**に絞る（長くしすぎない） |
| SC-P06 | 自動と手動を表で明示する。手動はプレビュー手順とセットで実施 |

### ファイル配置

| 種類 | 場所 |
|------|------|
| シナリオ文（ガーキン） | `e2e/features/*.feature` |
| ステップ実装 | `e2e/steps/*.ts` |
| 実行 | `npm run test:e2e` |
| 動画 | `test-results/**/*.webm` · `npm run test:e2e:report` |

---

## 3. シナリオ一覧（総括）

| ID | 機能領域 | シナリオ名 | 自動化 | feature ファイル |
|----|----------|------------|--------|------------------|
| SC-LP-01 | LP | ブランド名がヒーローに表示される | ✅ 自動 | `landing.feature` |
| SC-LP-02 | LP | Party Tools から3アプリへ遷移できる | ✅ 自動 | `landing.feature` |
| SC-LP-03 | LP | ヒーローにタグラインと CTA が表示される | ✅ 自動 | `landing.feature` |
| SC-LP-04 | LP | 課題と解決セクション（3柱）が表示される | ✅ 自動 | `landing.feature` |
| SC-POLL-01 | アンケート | Host がルームを自動作成し Guest が投票できる | ✅ 自動 | `poll.feature` |
| SC-POLL-02 | アンケート | Guest 投票後、Host が結果を表示できる | ✅ 自動 | `poll.feature` |
| SC-POLL-03 | アンケート | Host は選択肢を押せない（投票不可） | ✅ 自動 | `poll.feature` |
| SC-POLL-04 | アンケート | Host が質問を編集すると Guest にも反映 | ✅ 自動 | `poll.feature` |
| SC-POLL-05 | アンケート | Host に削除期限が表示され延長できる | ✅ 自動 | `poll.feature` |
| SC-BINGO-01 | ビンゴ | 名前入力でビンゴ達成し日時が表示される | ✅ 自動 | `bingo.feature` |
| SC-QUIZ-01 | クイズ | 共有 URL を開くと同じ問題セットになる | ✅ 自動 | `quiz.feature` |
| SC-TOOLS-01 | ツール群 | LP の `#tools` から各ツールへ遷移できる | ✅ 自動 | `tools.feature` |
| SC-TOOLS-02 | ツール群 | ビンゴ数字抽選機で数字を1つ抽選できる | ✅ 自動 | `tools.feature` |
| SC-TOOLS-06 | ツール群 | 入力不足はダイアログではなく画面上で理由が分かる | ✅ 自動 | `tools.feature` |
| SC-TOOLS-07 | ツール群 | 当選の記録をコピーして共有できる | ✅ 自動 | `tools.feature` |
| SC-TOOLS-09 | ツール群 | 得点板が押し間違えた加点を取り消せる | ✅ 自動 | `tools.feature` |
| SC-TOOLS-10 | ツール群 | 抽選機が直前の抽選を取り消せる | ✅ 自動 | `tools.feature` |
| SC-DRESS-01 | ドレス色当て | Admin が投票を開き Guest が色を選べる | ✅ 自動 | `live.feature` |
| SC-DEPLOY-01 | 運用 | 非本番／本番で上記が再現できる | 手動 | （[preview.md](./ops/preview.md)） |

---

## 4. シナリオ詳細

各シナリオは次の形式です。

- **ゴール**: 利用者が達成したいこと  
- **前提 / 操作 / 期待**: 合格条件  
- **自動化**: 対応するガーキンとステップ  

---

### SC-LP-01 — ブランド名がヒーローに表示される

| 項目 | 内容 |
|------|------|
| ゴール | 紹介ページを開いた人が「ことほぎ」だと分かる |
| 前提 | アプリ（または E2E 用サーバー）が起動している |
| 操作 | LP（`/`）を開く |
| 期待 | 画面上に「ことほぎ」と表示される |
| 自動化 | ✅ `landing.feature` |

```gherkin
シナリオ: ブランド名がヒーローに表示される
  もし LPを開く
  ならば 画面に "ことほぎ" と表示される
```

---

### SC-LP-02 — Party Tools から3アプリへ遷移できる

| 項目 | 内容 |
|------|------|
| ゴール | LP からビンゴ・クイズ・アンケートに進める |
| 前提 | LP を開いている |
| 操作 | ToolsGrid（`#tools`）内の各アプリへのリンクを開く |
| 期待 | それぞれ該当アプリの画面タイトル／主要 UI が見える |
| 自動化 | ✅ `landing.feature` |

| リンク名（目安） | 遷移先 | 期待する目印 |
|------------------|--------|--------------|
| 人間ビンゴ | `/app-tools/wedding-bingo/...` | 「Wedding Bingo」など |
| 新郎新婦クイズ | `/app-tools/wedding-quiz/...` | 「Wedding Quiz」など |
| リアルタイムアンケート | `/app-tools/wedding-poll/...` | 「Live Poll」または入室 UI |

---

### SC-LP-03 — ヒーローにタグラインと CTA が表示される

| 項目 | 内容 |
|------|------|
| ゴール | LP を開いた人が、何のサービスか・どこへ進めばよいか分かる |
| 前提 | アプリ（または E2E 用サーバー）が起動している |
| 操作 | LP（`/`）を開く |
| 期待 | タグラインと CTA「余興ツールを見る」が1つ見える（副 CTA なし） |
| 自動化 | ✅ `landing.feature` |

```gherkin
シナリオ: ヒーローにタグラインとCTAが表示される
  もし LPを開く
  ならば 画面に "インストール不要。会場のスマホで余興がつながる" と表示される
  かつ 画面に "余興ツールを見る" と表示される
```

---

### SC-LP-04 — 課題と解決セクション（3柱）が表示される

| 項目 | 内容 |
|------|------|
| ゴール | 実在する余興機能に沿った課題→解決が伝わる |
| 前提 | LP を開いている |
| 操作 | （スクロール不要。DOM 上に存在すれば可） |
| 期待 | 「配る手間」「手元の参加」「会場の一体感」の見出しが見える |
| 自動化 | ✅ `landing.feature` |

```gherkin
シナリオ: 課題と解決セクションが表示される
  もし LPを開く
  ならば 画面に "配る手間" と表示される
  かつ 画面に "手元の参加" と表示される
  かつ 画面に "会場の一体感" と表示される
```

---

### SC-DRESS-01 — Admin が投票を開き Guest が色を選べる

| 項目 | 内容 |
|------|------|
| ゴール | ドレス色当てで Admin がルームを作り、Guest が投票できる（L-DRESS） |
| 操作 | Admin がルーム自動作成 → VOTING → Guest が色を選択 |
| 期待 | 得票が1以上 |
| 自動化 | ✅ `live.feature` |

---

### SC-POLL-01 — Host がルームを自動作成し Guest が投票できる

| 項目 | 内容 |
|------|------|
| ゴール | 司会がルームを作り、ゲストが投票できる |
| 前提 | アンケート開始画面。Host モードでは司会向け使い方が見える（Guest モードでは隠れる） |
| 操作 | Host が「ルームを作成して入室」→ ゲスト URL を別ブラウザ相当で開く → 先頭の選択肢で投票 |
| 期待 | Host に質問編集ボタンがある。Guest に「投票済み」が表示される |
| 自動化 | ✅ `poll.feature` |
| ルーム | 実行ごとにサーバー側でユニークな4文字を自動発行 |

---

### SC-POLL-02 — Guest 投票後、Host が結果を表示できる

| 項目 | 内容 |
|------|------|
| ゴール | 司会が結果を会場に見せられる |
| 前提 | SC-POLL-01 と同様に Guest が1票入れたあと |
| 操作 | Host が「結果を表示」を押す |
| 期待 | 結果エリアが見え、票数が反映されている（合計が1以上） |
| 自動化 | ✅ `poll.feature` |

---

### SC-POLL-03 — Host は選択肢を押せない（投票不可）

| 項目 | 内容 |
|------|------|
| ゴール | 司会が誤って票を入れられない |
| 前提 | Host としてセッション画面を表示している |
| 操作 | 選択肢が無効であることを確認し、強制クリックしても API 上の票が増えないことを確かめる |
| 期待 | 選択肢は disabled。ルームの票合計は 0 のまま |
| 自動化 | ✅ `poll.feature` |

---

### SC-POLL-04 — Host が質問を編集すると Guest にも反映される

| 項目 | 内容 |
|------|------|
| ゴール | 司会が質問文を変え、ゲスト画面にも届く（要件 P-10） |
| 前提 | Host としてセッションに入室し、ゲスト用 URL がある |
| 操作 | 「質問・選択肢を編集」で先頭の質問文を変更して保存 → Guest が URL を開く |
| 期待 | Host / Guest 双方の質問文に編集内容が表示される（票はリセットされる） |
| 自動化 | ✅ `poll.feature` |

---

### SC-POLL-05 — Host に削除期限が表示され延長できる

| 項目 | 内容 |
|------|------|
| ゴール | 司会がデータの削除期限を把握し、必要なら延長できる（要件 P-11） |
| 前提 | Host としてセッション画面を表示している |
| 操作 | 削除期限の表示を確認 →「削除期限を1週間延長」を押す |
| 期待 | 期限文言が見える。延長後も期限が表示され、API 上の `expiresAt` が進む |
| 自動化 | ✅ `poll.feature` |

---

### SC-BINGO-01 — 名前入力でビンゴ達成し日時が表示される

| 項目 | 内容 |
|------|------|
| ゴール | マスを埋めてビンゴになり、達成時刻が見える |
| 前提 | ビンゴ画面。幹事用編集画面に使い方の要点がある。中央 FREE は「主役の2人」で埋まっている |
| 操作 | 中央を含む1ラインが揃うよう、残りマスに名前を登録する（例: 中央行の左右） |
| 期待 | ビンゴ達成の表示が出る。達成日時（日付・時刻）が表示される |
| 自動化 | ✅ `bingo.feature` |

---

### SC-QUIZ-01 — 共有 URL を開くと同じ問題セットになる

| 項目 | 内容 |
|------|------|
| ゴール | 幹事が配った URL で、ゲストも同じ問題を解ける |
| 前提 | クイズ画面。幹事用編集画面に使い方の要点がある |
| 操作 | 問題を識別できる内容に編集し共有 URL を作る → 別コンテキストでその URL を開いてクイズ開始 |
| 期待 | 同じ問題文（または編集した目印）が表示される |
| 自動化 | ✅ `quiz.feature` |

---

### SC-TOOLS-01〜02 — 幹事・進行ツール群（機能が成立するか）

| 項目 | 内容 |
|------|------|
| ゴール | 追加ツール（TOOL-\*）が一覧から開け、主要操作が成立する |
| 前提 | LP（`/#tools`）または各ツールページを開ける |
| 自動化 | ✅ `tools.feature` |

| ID | シナリオ | 主な操作 | 期待 |
|----|----------|----------|------|
| SC-TOOLS-01 | LP 一覧から遷移 | LP を開く → `#tools` の「ビンゴ数字抽選機」を開く | LP に主要ツールのリンクがあり、抽選機画面へ遷移する |
| SC-TOOLS-02 | 抽選機で抽選 | 抽選機を開く → 「抽選する」 | 使い方が表示され、数字が1つ出て抽選済み数が 1 になる |

代表ツールを自動化し、残りのツールは共通ロジック（UT-PARTY-\*）と目視（L3）で担保する。  
旧ハブのキーワード絞り込み（旧 SC-TOOLS-08）は、一覧を LP に一本化したため廃止。  
（旧 SC-TOOLS-03〜05 は対象ツール廃止に伴い削除）

---

### SC-TOOLS-06〜10 — 幹事・進行ツール群（当日使えるか）

機能が動くことと、**マイクを持ったまま当日使えること**は別なので、操作性の約束（要件 T-07〜T-09）を独立したシナリオとして固定する。ツールごとに同じ作りを共有しているため（design §5.3b）、代表 1 ツールで守れていれば全体の回帰を検知できる。

| 項目 | 内容 |
|------|------|
| ゴール | 入力不足・押し間違い・結果の持ち帰りが、進行を止めずに扱える |
| 前提 | 各ツールページを開ける。localStorage は初期化した状態 |
| 自動化 | ✅ `tools.feature` |

| ID | シナリオ | 主な操作 | 期待 |
|----|----------|----------|------|
| SC-TOOLS-06 | 入力不足の伝え方 | ルーレットを開く → 候補を1件だけにする | 回すボタンが無効になり、件数（1件）が画面に出る。ダイアログは出ない |
| SC-TOOLS-07 | 結果の持ち帰り | ルーレットを回す → 「記録をコピー」 | 当選が表示され、「当選の記録をコピーしました」の通知が出る |
| SC-TOOLS-09 | 加点の取り消し | 得点板を開く → 先頭チームに加点 → 「1つ元に戻す」 | 得点が 1 になった後、0 に戻る |
| SC-TOOLS-10 | 抽選の取り消し | 抽選機を開く → 「抽選する」→「1つ取り消す」 | 抽選済みが 1 → 0 に戻り、表示も数字でなくなる |

---

### SC-DEPLOY-01 — 非本番／本番での再現（手動）

| 項目 | 内容 |
|------|------|
| ゴール | 会場用環境でも同じ体験ができる |
| 自動化 | ❌ 手動（L3） |
| 手順 | [ops/preview.md](./ops/preview.md) で非本番確認後、`main` へ |

チェックリスト（抜粋）:

- [ ] プレビューで SC-LP-02 / SC-POLL-01〜04 / SC-BINGO-01 / SC-QUIZ-01 相当を人手でも一度通した  
- [ ] 問題なければ `main` へマージ  

---

## 5. トレーサビリティ

| シナリオ | 要件 ID（目安） | 自動テスト層 |
|----------|-----------------|--------------|
| SC-LP-01 | LP-01 | L2b |
| SC-LP-02 | LP-03、受け入れ1 | L2b |
| SC-LP-03 | LP-01、LP-05 | L2b（+ UT-SITE-* が L1 で補完） |
| SC-LP-04 | LP-02 | L2b（+ UT-SITE-* が L1 で補完） |
| SC-POLL-01 | P-02〜P-06、受け入れ4 | L2b |
| SC-POLL-02 | P-09、受け入れ4 | L2b |
| SC-POLL-03 | P-03、受け入れ5 | L2b |
| SC-POLL-04 | P-10 | L2b |
| SC-POLL-05 | P-11 | L2b（+ UT-TTL-*） |
| SC-BINGO-01 | B-02〜B-04、受け入れ2 | L2b |
| SC-QUIZ-01 | Q-05、受け入れ3 | L2b |
| SC-TOOLS-01〜02 | T-01〜T-05 | L2b（+ UT-PARTY-* が L1 で補完） |
| SC-TOOLS-06〜07・09〜10 | T-05、T-07〜T-09 | L2b（+ UT-UI-* が L1 で補完） |
| SC-DRESS-01 | L-DRESS | L2b |
| SC-DEPLOY-01 | 受け入れ6 | L3 手動 |

---

## 6. 変更管理

- シナリオの追加・合格条件の変更は **この仕様書を更新**する  
- `.feature` / ステップは仕様と同時に直す  
- 単体・スモークの話は [test-spec.md](./test-spec.md) 側  
- 実行タイミング（push 時強制など）は [ops/testing.md](./ops/testing.md)  
